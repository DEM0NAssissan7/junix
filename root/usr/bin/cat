this.main = function(args) {
    let fd = fopen(args, "r");
    let data = read(fd);
    if(typeof data === "string" || typeof data === "function")
        printf(data + '\n');
    else
        printf(JSON.stringify(data) + "\n");
    fclose(fd);
    exit();
}